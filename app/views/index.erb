
<h2>This be da index/homepage</h2>

<% if current_user && updates.any? %>
	<% updates.each do |update| %>
		<% if update.class == List %>
			<%= erb :'/lists/_show', locals: {list: update} %>
		<% else %>
			<h2><%= update.movie.title %></h2>
		<% end %>
	<% end %>

<% end %>
