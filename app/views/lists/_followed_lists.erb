<div class="list-container followed-lists">
  <% if lists %>
  	<% lists.each do |list| %>
  		<%= erb :'/lists/_show', locals: {list: list} %>
  	<% end %>
  <% else %>
  	<h4>You have no favorite lists yet.</h4>
  <% end %>
</div>
